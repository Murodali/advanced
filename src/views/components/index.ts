import { Page } from "./page.component";
import { Grid } from "./grid.comonent";
import { IF } from "./if.component";

const Components = Object.freeze(
  Object.seal({
    Page,
    Grid,
    IF,
  })
);

export default Components;
