import { AppDispatch } from "../../app/store";
import { Api_Auth_Login_Body } from "../interfaces/api.interface";
import * as i from "../interfaces/api.interface";

const login = (body: Api_Auth_Login_Body):any => {
    async (dispatch: AppDispatch) => {
        try{
            const {status_code, message, payload}:i. 

        }catch(error:any){

        }
    }
};
